# Database Configuration
# Format: postgresql://[username]:[password]@[host]:[port]/[database_name]
# Example: postgresql://postgres:password@localhost:5432/campustrack
DATABASE_URL=postgresql://username:0552321295@localhost:5432/campustrack

# Server Configuration
PORT=5000
NODE_ENV=development

# Session Secret (CHANGE THIS IN PRODUCTION!)
# Generate a random string for production: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-random-secret-key-in-production
